# Call Graph 工具 - 快速开始

## 📦 安装依赖

### 方式 1: 使用 pip（推荐）

```bash
cd /path/to/call_graph
pip install -e .
```

### 方式 2: 使用虚拟环境（推荐用于开发）

```bash
cd /path/to/call_graph

# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows

# 安装依赖
pip install -e .
```

## 🚀 基本使用

### 1. 分析项目

```bash
# 分析项目（清空旧数据）
python call-graph.py --database myproject.db analyze /path/to/project --clear

# 排除特定目录
python call-graph.py --database myproject.db analyze /path/to/project --exclude "node_modules,build"
```

### 2. 查看统计信息

```bash
python call-graph.py --database myproject.db stats
```

### 3. 搜索函数

```bash
# 搜索函数名包含 "process" 的函数
python call-graph.py --database myproject.db search "process"

# 显示详细信息
python call-graph.py --database myproject.db search "calculate" --verbose
```

### 4. 查询调用关系

```bash
# 查询谁调用了某个函数
python call-graph.py --database myproject.db query validate_input --callers

# 查询某个函数调用了哪些函数
python call-graph.py --database myproject.db query process_data --callees

# 查询调用链（向下，深度3）
python call-graph.py --database myproject.db query main --chain --depth 3

# 查询完整调用路径（向上+向下）
python call-graph.py --database myproject.db query process_data --fullpath

# 显示详细信息
python call-graph.py --database myproject.db query calculate --fullpath --verbose
```

### 5. 导出调用图

```bash
# 导出为 DOT 格式
python call-graph.py --database myproject.db export --output graph.dot

# 使用 Graphviz 生成图片
dot -Tpng graph.dot -o graph.png
```

## 💡 快速演示

```bash
# 运行完整演示
./demo.sh

# 运行快速演示
./quick_demo.sh
```

## 📚 支持的语言

- Python
- C / C++
- Java
- Rust
- JavaScript / TypeScript
- Go

## 🔧 其他运行方式

### 使用 Python 模块方式

```bash
python -m call_graph --database myproject.db stats
```

### 安装到系统后使用

```bash
# 安装到系统
pip install -e .

# 直接使用命令
call-graph --database myproject.db stats
call-graph --database myproject.db query main --fullpath
```

## 📖 详细文档

- `README.md` - 项目总览
- `使用说明.md` - 详细使用文档
- `examples/` - 示例代码

## ⚠️ 注意事项

1. `--database` 参数必须放在子命令之前
2. 首次使用需要先安装依赖
3. 首次分析建议使用 `--clear` 清空旧数据
4. 默认排除 `node_modules`, `.git`, `__pycache__` 等目录

## 🆘 常见问题

### Q: ModuleNotFoundError: No module named 'tree_sitter'

**A:** 依赖未安装，运行：
```bash
pip install -e .
```

### Q: 使用 python 还是 python3？

**A:** 取决于你的系统：
- macOS/Linux 通常使用 `python3`
- 某些系统 `python` 指向 Python 2.x

建议使用 `python3` 以确保使用 Python 3.x

### Q: 如何分析大型项目？

**A:** 使用排除选项：
```bash
python call-graph.py --database large.db analyze /path/to/project \
  --exclude "node_modules,venv,build,dist,.git"
```

## 🎯 示例工作流

```bash
# 1. 安装依赖
pip install -e .

# 2. 分析示例项目
python call-graph.py --database demo.db analyze examples/sample_project --clear

# 3. 查看统计
python call-graph.py --database demo.db stats

# 4. 搜索函数
python call-graph.py --database demo.db search "main"

# 5. 查询完整路径
python call-graph.py --database demo.db query main --fullpath --verbose

# 6. 导出图表
python call-graph.py --database demo.db export --output demo.dot
dot -Tpng demo.dot -o demo.png
```

## 🌟 下一步

- 运行 `./demo.sh` 查看完整功能演示
- 查看 `examples/sample_project/` 了解示例项目结构
- 阅读 `使用说明.md` 了解高级功能

祝使用愉快！🚀

